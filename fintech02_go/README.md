# 핀테크 강의 로드맵 (3~4주차) — Go 기초 + REST API 개발

## 🗓️ Day 1: 개발 환경 설정과 Hello World
- **강의 자료**:
  - Go 개발 환경 설치 (Go, IDE, 터미널 설정)
  - `go run`, `go build` 명령어
  - 첫 `Hello Fintech` 프로그램 작성
- **실습 예제 (5)**:
  - Hello Fintech 출력
  - 이름과 나이를 변수로 저장해 출력
  - `fmt.Printf()`로 형식을 지정해 출력
  - 표준 입력으로 이름과 나이를 입력받아 출력
  - 원화로 입력받은 값을 그대로 출력

---

## 🗓️ Day 2: 변수, 타입, 조건과 반복
- **강의 자료**:
  - 기본 타입 (`string`, `int`, `float64`, `bool`)
  - 조건문 (`if`), 반복문 (`for`)
- **실습 예제 (5)**:
  - 원화 입력 후 달러로 변환
  - 점수 입력 후 학점 결정
  - 홀수/짝수 판단
  - 1부터 100까지의 합 구하기
  - 구구단 출력하기

---

## 🗓️ Day 3: 함수 정의, 반환 값
- **강의 자료**:
  - 함수 정의하기
  - 반환 값과 스코프
  - 여러 반환 값
- **실습 예제 (5)**:
  - 두 수를 더하는 함수
  - 원화 → 엔화 변환하는 함수
  - 최소값과 최대값 반환하는 함수
  - 원형 면적 계산하는 함수
  - 삼각형 넓이를 반환하는 함수

---

## 🗓️ Day 4: Struct 기초
- **강의 자료**:
  - Struct 정의하기
  - Struct 생성, 필드 접근
  - JSON 직렬화/역직렬화
- **실습 예제 (5)**:
  - `User` Struct 정의하기
  - `User` 데이터를 JSON으로 변환하기
  - `Product` Struct 정의하기
  - JSON 데이터를 `Product`로 역직렬화
  - Struct 내 여러 데이터를 `fmt.Println()`으로 출력하기

---

## 🗓️ Day 5: 포인터와 메서드
- **강의 자료**:
  - 포인터 원리
  - 메서드 정의하기
  - Struct 메서드 작성
- **실습 예제 (5)**:
  - 포인터로 변수 변경하기
  - 포인터로 Struct 필드 변경하기
  - 원의 면적 계산 메서드 정의하기
  - 예금 메서드 정의하기
  - 출금 메서드 정의하기

---

## 🗓️ Day 6: Goroutine과 Channel
- **강의 자료**:
  - Goroutine 개념과 생성
  - Channel 생성과 메시지 전달
  - 비동기 로직 기초
- **실습 예제 (5)**:
  - Goroutine으로 메시지 출력하기
  - 채널 통해 메시지 전달하기
  - 여러 Goroutine 생성하기
  - 채널로 여러 값 전달하기
  - 종료 채널 만들기

---

## 🗓️ Day 7: 모듈과 패키지
- **강의 자료**:
  - 모듈 생성 (`go mod init`)
  - 패키지 작성하기
  - 외부 모듈 활용하기
- **실습 예제 (5)**:
  - 로컬 패키지 생성하기
  - 다른 파일에서 함수 호출하기
  - 모듈 파일 변경해 버전 관리하기
  - 외부 모듈 (`logrus`) 설치해 로거 생성하기
  - 패키지를 통해 수학 연산 호출하기

---

## 🗓️ Day 8: REST API 기초 (`net/http`)
- **강의 자료**:
  - `net/http`로 서버 생성
  - Route 정의하기
  - JSON 반환하기
- **실습 예제 (5)**:
  - `GET /hello`로 "Hello" 반환하기
  - `GET /user`로 JSON 반환하기
  - `POST /user`로 데이터 수신 후 반환하기
  - `GET /items`로 아이템 목록 반환하기
  - `GET /status`로 헬스 체크 반환하기

---

## 🗓️ Day 9: REST API 개발 실습 (회원가입 기초)
- **강의 자료**:
  - JSON Request 처리
  - Struct로 Request 정의하기
  - 회원가입 로직 작성
- **실습 예제 (5)**:
  - `POST /signup`으로 JSON 데이터를 수신하기
  - 비밀번호 최소 글자 수 체크하기
  - 이메일 형식을 체크하기
  - 성공 메시지 반환하기
  - 에러 메시지 반환하기

---

## 🗓️ Day 10: REST API 개발 실습 (로그인 기초)
- **강의 자료**:
  - 로그인 Request 정의하기
  - 데이터 검증 로직 개발
  - 로그인 성공/실패 반환하기
- **실습 예제 (5)**:
  - `POST /login` 성공 케이스 반환하기
  - `POST /login` 실패 케이스 반환하기
  - 비밀번호 누락 케이스 반환하기
  - 아이디 누락 케이스 반환하기
  - 로직 디버그 메시지 출력하기

---

## 🗓️ Day 11: REST API 개발 실습 (회원 데이터 저장)
- **강의 자료**:
  - 메모리 내 저장소 생성 (`map`)
  - 회원가입 시 데이터 저장
  - 로그인 시 데이터 검증
- **실습 예제 (5)**:
  - `map[string]string`으로 아이디/비번 저장하기
  - 저장된 데이터를 통해 로그인 로직 개발
  - 가입 후 저장된 데이터를 JSON으로 반환하기
  - 저장된 데이터를 통해 로그인 성공 메시지 반환하기
  - 저장된 데이터를 통해 로그인 실패 메시지 반환하기

---

## 🗓️ Day 12: REST API 개발 실습 (회원 삭제, 조회)
- **강의 자료**:
  - `DELETE /user` 개발
  - `GET /user` 개발
  - 저장된 데이터를 통해 삭제 로직 개발
- **실습 예제 (5)**:
  - 아이디로 회원 삭제하기
  - 저장된 회원 모두 반환하기
  - 저장된 회원 한 명 반환하기
  - 삭제 실패 케이스 개발하기
  - 삭제 성공 케이스 개발하기

---

## 🗓️ Day 13: REST API 개발 실습 (구조화된 프로젝트 개발)
- **강의 자료**:
  - 폴더별 파일 구조 정의하기
  - 핸들러 파일과 로직 파일 분리하기
  - `main.go` 최소화하기
- **실습 예제 (5)**:
  - 핸들러 파일 생성하기
  - 로직 파일 생성하기
  - `main.go`로 핸들러 호출하기
  - 로직 변경 후 핸들러 호출하기
  - 로거를 통해 로직 추적하기

---

## 🗓️ Day 14: REST API 개발 실습 (최종 점검)
- **강의 자료**:
  - 전체 REST API 점검
  - 로컬 개발 환경 점검
  - 문서화 방법 정리
- **실습 예제 (5)**:
  - `curl`로 회원가입 호출하기
  - `curl`로 로그인 호출하기
  - 삭제된 아이디로 로그인해 실패 케이스 호출하기
  - `GET /user`로 전체 회원 출력하기
  - 에러 케이스 강제로 발생해 로거로 추적하기
